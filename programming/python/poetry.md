Configure in project virtual environment

`poetry config virtualenvs.in-project true`

Install

`poetry install`

Project setup

`poetry new <folder-name> --name <package-name>`

Add dependencies

`poetry add <dependencies>`

